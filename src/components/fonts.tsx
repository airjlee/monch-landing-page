import localFont from 'next/font/local'
 
// Font files can be colocated inside of `pages`
export const fago = localFont({ src: './fagopro.otf' });

export const monch = localFont({ src: './fagoprobold.otf' });